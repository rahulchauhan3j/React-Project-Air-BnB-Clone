import photoGrid from "../images/photo-grid.png";

function PhotoGrid() {
  return (
    <div className="PhotoGrid">
      <img src={photoGrid} className="PhotoGridImage"></img>
    </div>
  );
}

export default PhotoGrid;
